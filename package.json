{
  "name": "veridica-rendertron-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Plain Rendertron CLI service",
  "scripts": {
    "start": "rendertron --port $PORT --timeout 15000"
  },
  "dependencies": {
    "rendertron": "3.1.0"
  },
  "engines": { "node": ">=20" }
}
